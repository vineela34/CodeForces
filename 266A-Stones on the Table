#include<cstdio>
int main(){
  int n,c=0,i=0;
  scanf("%d",&n);
  char a[n];
  scanf("%s",a);
  while(n--){
      if(a[i] == a[i+1])
        c++;
    else
        a[i]=a[i+1];
    i++;
  }
  printf("%d\n",c);
  return 0;
}
